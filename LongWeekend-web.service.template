[Unit]
Description=LongWeekend-web flask app
After=network.target

[Service]
User=%username%
Group=%groupname%
WorkingDirectory=%currentpath%
ExecStart=%currentpath%/.venv/bin/gunicorn --workers 2 --bind 0.0.0.0:5001 --timeout 240 run:app
Restart=always

[Install]
WantedBy=multi-user.target